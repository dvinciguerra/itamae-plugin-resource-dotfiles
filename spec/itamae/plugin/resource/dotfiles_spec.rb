# frozen_string_literal: true

RSpec.describe Itamae::Plugin::Resource::Dotfiles do
  it "has a version number" do
    expect(Itamae::Plugin::Resource::Dotfiles::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
